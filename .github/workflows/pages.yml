# The site is generated, then deployed. It used to be generated and COMMITTED:
# 1,057 per-puzzle pages, a hub, a sitemap and a patched homepage, all of which
# change whenever app.js or style.css does, because every one of them carries a
# content-hashed ?v= stamp. That made a one-line CSS fix a 1,059-file commit and
# put a five-minute rebuild in front of every push.
#
# So the repo holds the sources and this holds the build. Nothing under
# .gitignore's "generated by tools/build_seo_pages.py" block is committed any
# more; it is written here, on a clean checkout, and handed straight to Pages.
#
# The smoke test runs between the two, against the generated tree rather than
# the repo: the stamp sweep can only check pages that exist, so a build that
# wrote a stale stamp fails here instead of serving an unstyled page to anyone
# arriving from search.
name: Build and deploy the site

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# One deploy at a time, and the newest wins: two pushes a minute apart must not
# race to publish, and the older one is not worth waiting for.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Generate the crawlable pages
        run: python3 tools/build_seo_pages.py
      - name: Stamp the assets
        run: python3 tools/stamp_assets.py
      - name: Check the annotations
        run: python3 tools/validate_annotations.py
      - name: Smoke test
        run: node tools/smoke_test.js
      # Excludes .git and .github itself; everything else goes up as the site,
      # which is what Pages served from the branch before this.
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
